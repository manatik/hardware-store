version: "3.10"

x-node-version: &node-version node:18.0

services:
  db:
    image: postgres
    restart: always
    ports:
      - "25432:5432"
    environment:
      - POSTGRES_DB=test
      - POSTGRES_USER=test
      - POSTGRES_PASSWORD=testik
    volumes:
      - pgdata:/var/lib/postgresql/data
      - pgsql-sock:/var/run/postgresql
    networks:
      - web

  backend:
    image: *node-version
    container_name: nestjs
    ports:
      - "4200:4200"
    networks:
      - web
    depends_on:
      - db
    volumes:
      - ../backend:/api
      - pgsql-sock:/var/run/postgresql
      - ../backend/assets:/var/lib/images
    working_dir: /api
    command: bash -c "
      yarn install
      && yarn prisma generate
      && yarn prisma migrate dev
      && yarn prisma db seed
      && yarn start:dev"

  frontend:
    image: *node-version
    container_name: nextjs
    ports:
      - "3000:3000"
    networks:
      - web
    depends_on:
      - backend
    volumes:
      - ../frontend:/frontend
    working_dir: /frontend
    command: bash -c "
      yarn install
      && yarn dev"

volumes:
  pgdata:
  pgsql-sock:

networks:
  web:
